// Online C compiler to run C program online#include <stdio.h>int main() {    int num;    printf("숫자를 입력하세요 :");    scanf("%d",&num);    //조건문    if(num %2==0)    //if<- 물어 볼 때 사용함. %2==0의 % = 나머지 마지막에 세미콜론 사용하지 않기    //같다는 == 같지 않다는 !=    {printf("짝수");    printf("\n2로 나눈 나머지가 0");} //출력문을 2개 이상 사용하고 싶다면 {}사용하기    else    // if와else는 if는 ~가 ~라면이고 else는 ~가 ~가 아니라면 이라고 해석하면 편함    {printf("홀수");    printf("\n2로 나눈 나머지가 1");}    return 0;}