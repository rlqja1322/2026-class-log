#include <stdio.h>

int main() {
    int num;
    while(1)
    {
    printf("숫자 입력 하세요 : ");
    scanf("%d",&num);
    
    //while은 참일때만 반복, 0<- 거짓 1<- 참
    //while은 무한반복, {}쓸 때는 while 뒤부터 return0 앞까지
    
    if(num==10)
    //num은 ~보다 "크거나 작다"를 표현할때 num을 기준으로 괄호를 움직임
    {printf("정답입니다\n");
    break;}
    //break에 닿으면 탈출이라고 생각하면 편함, 물론 while이 있다면 다시 처음부터 시작함
    else if(num>10)
    //else if는 선택지를 추가 할때 사용함
    printf("너무 높아요\n");
    else 
    printf("너무 낮아요\n");
    }
    return 0;
    }// 항상 괄호 개수 맞추기@@@@필수@@@@